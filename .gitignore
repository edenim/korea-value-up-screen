# Secrets
.env

# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Data: raw API responses are cached locally, never committed
data/raw/*
!data/raw/.gitkeep

# OS / Office temp files
.DS_Store
~$*.xlsx
~$*.docx

# OpenDartReader writes its corp-code cache here
docs_cache/

# Local session notes for Claude Code (kept on disk, not published)
CLAUDE.md
